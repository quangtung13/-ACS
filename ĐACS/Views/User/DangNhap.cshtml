@model ĐACS.Models.NGUOIDUNG

@{
    ViewBag.Title = "DangNhap";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <section class="vh-100" style="background-color: white;">
        <div class="container py-5 h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col col-xl-10">
                    <div class="card" style="border-radius: 1rem;">
                        <div class="row g-0">
                            <div class="col-md-6 col-lg-5 d-none d-md-block">
                                <img src="~/Content/images/pexels-vincent-rivaud-2227832.jpg"
                                     alt="login form" class="img-fluid" style="border-radius: 1rem 0 0 1rem;"/>
                            </div>
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            <div class="col-md-6 col-lg-7 d-flex align-items-center">
                                <div class="card-body p-4 p-lg-5 text-black">

                                    <form>
                                        <h5 class="fw-normal mb-3 pb-3" style="letter-spacing: 1px;">Đăng nhập vào tài khoản của bạn</h5>

                                        <div class=" mb-4">
                                            @Html.EditorFor(model => model.TENDANGNHAP, new { htmlAttributes = new { @class = "form-control" } })
                                            @ViewData["login"]
                                            @Html.ValidationMessageFor(model => model.TENDANGNHAP, "", new { @class = "text-danger" })
                                            <label class="form-label" for="form2Example17">Tên đăng nhập</label>
                                        </div>

                                        <div class=" mb-4">
                                            @Html.EditorFor(model => model.MATKHAU, new { htmlAttributes = new { type = "Password", @class = "form-control" } })
                                            @ViewData["login"]
                                            @Html.ValidationMessageFor(model => model.MATKHAU, "", new { @class = "text-danger" })
                                            <label class="form-label" for="form2Example27">Mật khẩu</label>
                                        </div>

                                        <div class="pt-1 mb-4">
                                            <input type="submit" value="Đăng nhập" class="btn btn-dark btn-lg btn-block" />
                                        </div>

                                        <a class="small text-muted" href="#!">Quên mật khẩu?</a>
                                        <p class="mb-5 pb-lg-2" style="color: #393f81;">
                                            Bạn chưa có tài khoản?  @Html.ActionLink("Đăng ký", "DangKy", "User", new { area = "" }, new { @class = "inline" })
                                        </p>
                                    </form>

                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
}
